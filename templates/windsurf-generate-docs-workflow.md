---
auto_execution_mode: 0
description: Generate comprehensive documentation for this repo (and optionally its group) using the graphify knowledge graph as a navigation aid.
---

Run the generate-docs skill.

1. Read the full step-by-step instructions from `~/.codeium/windsurf/skills/generate-docs/SKILL.md`.
2. Follow the orchestrator's flow: read each per-pass prompt from `~/.codeium/windsurf/skills/generate-docs/prompts/` as you enter it.
3. If `~/.codeium/windsurf/skills/generate-docs/SKILL.md` is not found, tell the user to run `gfleet skills install` and retry.
