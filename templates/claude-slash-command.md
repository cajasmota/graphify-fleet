---
description: Generate comprehensive, navigable documentation for this codebase or its group. Uses the graphify knowledge graph for navigation. Multi-pass workflow with plan-before-write, incremental updates, and cross-repo links.
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, mcp__graphify-*
---

# /generate-docs

You have a skill at `~/.claude/skills/generate-docs/SKILL.md` that does this work. Follow it.

User invocation:
```
$ARGUMENTS
```

Read `~/.claude/skills/generate-docs/SKILL.md` and follow its instructions exactly. The skill is modular — read each pass file from `~/.claude/skills/generate-docs/prompts/<NN>-*.md` when you enter that pass.
