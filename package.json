{
  "name": "graphify-fleet",
  "version": "0.2.0",
  "description": "Orchestrate graphify across multiple related repos with isolated cross-repo graphs.",
  "type": "module",
  "bin": {
    "gfleet": "./bin/gfleet"
  },
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test 'test/*.test.js'",
    "test:watch": "node --test --watch 'test/*.test.js'"
  },
  "engines": {
    "node": ">=18.19"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0"
  },
  "files": [
    "bin",
    "src",
    "templates",
    "examples",
    "README.md"
  ]
}
