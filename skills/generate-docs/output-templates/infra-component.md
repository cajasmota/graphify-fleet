<!-- Template: infra/components/<name>.md (Terraform/CDK/Pulumi component). -->
<!-- Subagent: copy, fill, then verify. -->

<!-- docs:auto -->
# {{Component name — e.g., "Networking", "Compute", "Databases"}}

<!-- auto:start id=summary -->
{{Paragraph: what this component groups + the cloud platform.}}

| | |
|---|---|
| **Cloud** | {{AWS / GCP / Azure / multi}} |
| **Source** | [`{{path/to/dir}}`]({{rel}}) |
| **Resources** | {{count of `resource` blocks or CDK constructs}} |
| **Environments** | {{e.g., "dev / staging / production"}} |
<!-- auto:end -->

<!-- auto:start id=resources -->
## Resources

For each significant resource (or resource group), one section.

### {{ResourceTypeOrName}}

> {{What it provides + which app(s) consume it.}}

| | |
|---|---|
| **Type** | `aws_vpc`, `aws_security_group`, etc. (Terraform) — or `Vpc`, `SecurityGroup` (CDK construct) |
| **Source** | [`{{path}}:{{line}}`]({{rel-link}}) |
| **Per-environment differences** | {{e.g., "prod has 3 AZs, dev has 1"}} |

<details open>
<summary><b>⚙️ How it works</b></summary>

{{Plain prose: what this resource does, why it's configured this way, how it relates to other resources.

Example for a VPC: "The main VPC has 3 public subnets and 3 private subnets across 3 AZs. Public subnets host the ALBs; private subnets host ECS tasks and RDS instances. NAT Gateway in each public subnet provides egress for private resources. Flow logs are enabled and shipped to a central log bucket for compliance."}}

</details>

<details>
<summary><b>🔧 Key configuration</b></summary>

| Key | Value | Why |
|-----|-------|-----|
| `cidr_block` | `10.0.0.0/16` | Standard /16 sized for ~65k IPs |
| `enable_dns_hostnames` | `true` | Required for RDS endpoint resolution |
| `enable_flow_log` | `true` | Compliance |

</details>

<details>
<summary><b>📦 Outputs / consumers</b></summary>

- `vpc_id` → consumed by `compute` (ECS), `databases` (RDS), `networking` (peering)
- `private_subnet_ids` → consumed by `compute` (Fargate placement)

</details>

::: tip Source
[`{{path}}`]({{rel-link}}#L{{line}})
:::

---

{{repeat for each resource group}}
<!-- auto:end -->

<!-- auto:start id=footer -->
*Generated by `/generate-docs`. Last regenerated: {{ISO-date}}.*
<!-- auto:end -->
