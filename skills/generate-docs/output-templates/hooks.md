<!-- Template: hooks.md (React/RN custom hooks) or composables.md (Vue). -->
<!-- Subagent: copy, fill, then verify. -->

<!-- docs:auto -->
# {{Module}} — {{Hooks | Composables}}

<!-- auto:start id=summary -->
{{Paragraph: what kind of hooks/composables this module exports + the dominant data-fetching library if any (TanStack Query, SWR, etc.)}}

| | |
|---|---|
| **Source dir** | [`{{path}}`]({{rel}}) |
| **Data fetching** | {{e.g., "TanStack Query v5"}} / "manual fetch" / "SWR" |
| **State coupling** | {{e.g., "writes to `useInspectionStore` (zustand)"}} |
<!-- auto:end -->

<!-- auto:start id=hooks-toc -->
## Hooks ({{N}})

| Hook | At-a-glance |
|------|-------------|
| [`useInspections`](#useinspections) | List query; depends on filter store |
| [`useCreateInspection`](#usecreateinspection) | Mutation + optimistic update + cache invalidation |
| [`useInspectionDetails`](#useinspectiondetails) | Single-record query with prefetching |
<!-- auto:end -->

<!-- auto:start id=hooks -->
## Hooks

### `useInspections`

> {{One-line: what it returns and when to use it}}

| | |
|---|---|
| **Signature** | `useInspections(filter?: InspectionFilter): UseQueryResult<Inspection[]>` |
| **Source** | [`{{path}}:{{line}}`]({{rel-link}})  |
| **Type** | {{Data query / Mutation / State / Effect}} |

<details>
<summary><b>📋 Parameters</b></summary>

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `filter` | `InspectionFilter` | — | Optional filter; defaults to "all upcoming" |

</details>

<details>
<summary><b>📤 Returns</b></summary>

```ts
UseQueryResult<Inspection[]> {
  data: Inspection[] | undefined
  isLoading: boolean
  error: Error | null
  refetch: () => Promise<...>
}
```

</details>

<details>
<summary><b>🔑 Query key + cache</b> (data hooks only)</summary>

| | |
|---|---|
| **Query key** | `['inspections', filter]` |
| **Stale time** | 30 seconds |
| **Cache time** | 5 minutes |
| **Refetch on window focus** | yes |
| **Refetch triggers** | filter changes; manual `.refetch()` |
| **Cache invalidations** | `useCreateInspection`, `useDeleteInspection`, `useUpdateInspection` invalidate `['inspections']` |

(Skip block for non-data hooks.)

</details>

<details open>
<summary><b>⚙️ How it works</b></summary>

{{Plain prose. Same rules as the api-class template — natural language, NOT annotated code.

For data hooks: explain the query strategy, dependencies, side effects, error handling.
For mutations: optimistic update strategy, rollback on error, cache invalidations.
For state hooks: state shape, when it updates, persistence.
For effect hooks: when the effect runs, dependency analysis, cleanup.}}

Example walkthrough for `useCreateInspection`:

This is a TanStack Query mutation that wraps the `POST /api/v1/inspections/` call. On submit, it optimistically inserts the new inspection into the `['inspections']` cache so the list updates instantly. If the request succeeds, the cache is replaced with the server's authoritative version (canonical IDs, server timestamps). If it fails, the optimistic insert is rolled back and the error surfaces via the `onError` handler — the calling component shows a toast.

After success, the hook invalidates `['inspections']`, `['inspection-counts']`, and `['client', clientId, 'inspections']` so any nearby query that depends on the inspection list refetches automatically. The user-perceived latency is just the optimistic insert; everything else happens in the background.

</details>

<details>
<summary><b>📦 Side effects</b></summary>

- Reads from `useFilterStore` (zustand)
- Invalidates `['inspections']`, `['inspection-counts']` on success
- Writes to `useToastStore` on error

(Or "**None — pure query**" for read-only hooks.)

</details>

::: tip Source
[`useInspections`]({{rel-link}}#L{{line}})

Calls backend: [`GET /api/v1/inspections/`]({{cross-repo-link}}) (myapp-backend)
:::

---

{{repeat for every public hook}}
<!-- auto:end -->

<!-- auto:start id=footer -->
*Generated by `/generate-docs`. Last regenerated: {{ISO-date}}.*
<!-- auto:end -->
