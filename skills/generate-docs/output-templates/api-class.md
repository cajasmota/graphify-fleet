<!-- Template: per-ViewSet/controller/handler-group api file. -->
<!-- Subagent: copy this template, fill in placeholders, then run snippets/verification-checklist.md before returning. -->

<!-- docs:auto -->
# {{ClassName}}

<!-- auto:start id=summary -->
{{One paragraph: what this class owns + which models/resources it operates on. Use domain terms from docs-config.json vocabulary.}}

**Source**: [`{{source_path}}:{{class_start_line}}`]({{relative_source_link}})
**Auth (default)**: {{class-level permission classes — pulled from `permission_classes` attribute}}
**Mounted at**: {{base path from urls.py registration}}
<!-- auto:end -->

<!-- auto:start id=actions-checklist -->
## Action inventory (completeness checklist)

This {{ClassName}} has **{{N}} public actions**. Every item below MUST have a section in the "Actions" block. If you see this list and any item is missing below, the file is INCOMPLETE — re-run `/generate-docs --section <this-file>`.

- [ ] {{action_1}}
- [ ] {{action_2}}
- [ ] ...

(List every public method/action by name. Default DRF actions: list, retrieve, create, update, partial_update, destroy. Custom @action decorators each get their own line.)
<!-- auto:end -->

<!-- auto:start id=actions -->
## Actions

### {{HTTP_METHOD}} `{{path_template}}`

{{1-3 sentences: what it does in domain terms. Include any non-obvious R5 finding (counter semantics, dual-path strategy, param interactions) — don't bury them in later sections.}}

**Auth**: {{permission classes for this action — may override class default}}

**Path/query params**:
- `{{param_name}}` ({{type}}, {{required|optional}}) — {{what it does}}. {{If absent: behaviour}}. {{If changes execution path: how}}
- ... or "**None**" if no params

**Request body** (if applicable):
```json
{
  "field_1": "...",
  "field_2": 123
}
```

Per-field validation:
- `field_1` — {{rule}}
- `field_2` — {{rule}}

**Alternate flows** (any param combination causing fundamentally different behaviour):
- {{e.g., `view_all=true` returns assigned + available users vs assigned only}}
- ... or "**None — single flow**"

**Preconditions/gating** (model-state conditions for the endpoint to behave as described):
- {{e.g., active devices must exist on the building}}
- ... or "**None**"

**Response 2xx**:
```json
{ ... }
```

**Response 4xx/5xx**:
- 400 — {{when}}
- 404 — {{when}}
- 403 — {{when}}

**Side effects** (write actions only — beyond the primary record):
- {{e.g., creates ContractNote with author=request.user, system=True}}
- {{e.g., fires inspection.created signal}}
- {{e.g., queues send_invite celery task}}
- ... or "**None**"

**Handler**: [`{{ClassName}}.{{method_name}}`]({{relative_source_link}}#L{{line}})

**Cross-repo callers** (from merged graph):
- Mobile: [`{{caller_function}}`]({{relative_path_to_mobile_doc}}#{{anchor}})
- Frontend: [`{{caller_hook}}`]({{relative_path_to_frontend_doc}}#{{anchor}})
- ... or "**None — internal only**" / "**None — graph search returned no callers**"

---

{{repeat the section above for EVERY action listed in the inventory checklist}}
<!-- auto:end -->

<!-- auto:start id=footer -->
*Generated by `/generate-docs`. Last regenerated: {{ISO-date}}. Source SHA: {{file_sha}}.*
<!-- auto:end -->
