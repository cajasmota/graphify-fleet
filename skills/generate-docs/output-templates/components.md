<!-- Template: components.md for frontend (React/Vue/Svelte) and mobile (RN/Flutter). -->
<!-- Subagent: copy, fill, then verify. -->

<!-- docs:auto -->
# {{Module}} — Components

<!-- auto:start id=summary -->
{{Paragraph: what kind of components live here + which UI library/primitives they wrap, if any. Mention any module-specific design conventions.}}

| | |
|---|---|
| **Source dir** | [`{{path/to/components/}}`]({{rel}}) |
| **UI library** | {{e.g., "wraps gluestack-ui v2 primitives"}} / "custom from scratch" |
| **Styling** | {{e.g., "tailwind", "styled-components", "css-modules", "Stylesheet (RN)"}} |
| **Public components** | {{count — those used by other modules}} |
| **Internal components** | {{count — used only within this module; not documented individually}} |
<!-- auto:end -->

<!-- auto:start id=components-by-purpose -->
## Components (grouped by purpose)

Group by purpose, not alphabetically. Document **public** components (used outside this module) — internal-only components are listed at end with one-line each.

### Layout

#### `<DashboardShell>`

> {{One-line: what role this plays in screen composition}}

**Source**: [`{{path}}`]({{rel-link}})

<details open>
<summary><b>🧩 Props</b></summary>

```ts
interface DashboardShellProps {
  title: string;
  actions?: ReactNode;
  showBackButton?: boolean;
  onBack?: () => void;
  children: ReactNode;
}
```

| Prop | Required | Default | Purpose |
|------|----------|---------|---------|
| `title` | ✓ | — | Page title shown in header |
| `actions` | — | — | Right-side action buttons (e.g., "+ New", filters) |
| `showBackButton` | — | `false` | iOS-style back arrow + onBack |
| `onBack` | — | — | Required when `showBackButton=true` |

</details>

<details>
<summary><b>📡 Events / callbacks</b></summary>

- `onBack` (optional) — fired when back button pressed
- (Skip block if no events.)

</details>

<details>
<summary><b>🎰 Slots / children</b></summary>

- `children` — main content
- (Vue/Svelte equivalents: list named slots)

</details>

<details>
<summary><b>📌 When to use</b></summary>

Use as the outer wrapper for any module-level page. NOT for modal content (use `<DialogShell>` instead).

</details>

::: tip Source
[`DashboardShell`]({{rel-link}}#L{{line}})
:::

---

#### `<Section>`

> Container for a logical group of widgets within a page.

(Repeat per public layout component.)

### Data display

#### `<InspectionCard>`

> Compact representation of an Inspection in lists and dashboards.

(Same structure: Source / Props / Events / Slots / When to use / Source link.)

### Forms

#### `<InspectionForm>`

(Same structure. Forms get extra: validation strategy, submission handling.)

### Feedback

(Toasts, Spinners, Alerts.)

### Domain-specific

#### `<InspectionStatusBadge>`

> Renders the colored status pill for `Inspection.status`.

(Pure presentational; no events.)

<!-- auto:end -->

<!-- auto:start id=internal-components -->
## Internal-only components (not part of the module's public surface)

Brief one-line each. Don't expand to full sections — they're implementation details.

- `<InspectionRowActions>` — overflow menu used inside `<InspectionTable>`
- `<EmptyState>` — used by several screens, but only within this module
- `<DateRangePicker>` — composes shared `<DatePicker>` for inspection-scope ranges

<!-- auto:end -->

<!-- auto:start id=footer -->
*Generated by `/generate-docs`. Last regenerated: {{ISO-date}}.*
<!-- auto:end -->
